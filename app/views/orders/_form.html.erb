<%= form_for(order) do |f| %>
    <div class="form-group">
      <% if order.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

            <ul>
              <% order.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>


  <div class="field">
    <%= f.label :seller %>
    <%= f.label @order.book.user.login %>
  </div>

  <div class="field">
    <%= f.label :buyer %>
    <%= f.label @order.user.login %>
  </div>

  <div class="field">
    <%= f.label :admin %>
    <%= f.label @order.admin %>
  </div>

  <div class="actions", id="update">
    <%= f.submit  %>
  </div>
    </div>
<% end %>
